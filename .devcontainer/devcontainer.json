{
    "name": "Python 3 & PostgreSQL",
    "dockerComposeFile": "docker-compose.yml",
    "service": "app",
    "workspaceFolder": "/workspaces/${localWorkspaceFolderBasename}",
    "features": {
        "ghcr.io/devcontainers/features/node:1": {
            "nodeGypDependencies": true,
            "version": "lts"
        }
    },
    "forwardPorts": [
        3000,
        3001
    ],
    "onCreateCommand": "(cp -n .env.example .env || echo \".env creation failed\"); (pipenv install || echo \"pipenv install failed\"); (bash database.sh || echo \"database.sh failed\");",
    "postCreateCommand": "python docs/assets/greeting.py both || echo 'Greeting script skipped'",
    "customizations": {
        "vscode": {
            "extensions": [
                "esbenp.prettier-vscode",
                "ms-python.autopep8"
            ]
        }
    },
    "remoteUser": "vscode"
}